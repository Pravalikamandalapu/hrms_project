HRMS Frontend (Vite + React)
----------------------------
Quick start:
1. cd frontend
2. npm install
3. (optional) set VITE_API_BASE in .env to backend URL, e.g. VITE_API_BASE=http://localhost:5000
4. npm run dev
The demo comes with a seeded backend (see backend/README).
